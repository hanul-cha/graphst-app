overwrite: true
schema: 'http://localhost:4000/graphql'
documents: 'src/**/*.gql'
generates:
  src/api/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
